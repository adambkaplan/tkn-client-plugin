apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: s2i-go
  namespace: jenkins-tkn
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
